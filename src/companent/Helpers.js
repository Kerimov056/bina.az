// export const handleFilterChange = (filterValue, selectedFilters, setSelectedFilters) => {
//     if (selectedFilters.includes(filterValue)) {
//         const updatedFilters = selectedFilters.filter((filter) => filter !== filterValue);
//         setSelectedFilters(updatedFilters);
//     } else {
//         const updatedFilters = [...selectedFilters, filterValue];
//         setSelectedFilters(updatedFilters);
//     }
// };
